services:
  wellpaw-model:
    build: .
    container_name: wellpaw_model
    # command: uvicorn app.ml_server:app --host 0.0.0.0 --port 50002 --reload
    ports:
      - "50002:50002"
    environment:
      - APP_PORT=50002
      - PYTHONUNBUFFERED=1
    volumes:
      - ./model:/app/model
      - ./mapping:/app/mapping
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 7.5G